# Простой пример подключения к PostgreSQL

## Что делает этот пример?

1. Подключается к PostgreSQL
2. Настраивает Connection Pool
3. Выполняет простые SELECT запросы
4. Показывает работу с `QueryRowContext` и `QueryContext`
5. Демонстрирует JOIN между таблицами

## Запуск

### 1. Запустите PostgreSQL через Docker

В корневой папке lesson3:

```bash
cd ../..
docker-compose up -d
```

### 2. Установите зависимости

```bash
go mod download
```

### 3. Запустите пример

```bash
go run main.go
```

## Ожидаемый вывод

```
✅ Successfully connected to PostgreSQL!
📊 Total users in database: 2

👥 Users:
  [1] alice@example.com (created: 2025-01-07)
  [2] bob@example.com (created: 2025-01-07)

📝 Todos:
  ⬜ [1] Купить молоко (by alice@example.com)
  ⬜ [2] Сделать ДЗ (by alice@example.com)
  ✅ [3] Прочитать книгу (by bob@example.com)

✨ Done!
```

## Что изучить в коде?

- Строка подключения (DSN)
- Настройка Connection Pool
- `db.PingContext()` для проверки подключения
- `QueryRowContext()` для одной строки
- `QueryContext()` для нескольких строк
- `defer rows.Close()` — обязательно!
- Обработка ошибок
- JOIN между таблицами
